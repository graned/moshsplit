# API Configuration
# The base URL for the MoshSplit API (Sentinel)
VITE_API_BASE_URL=http://localhost:9000

# Sentinel Auth Service URL (if different from API)
VITE_SENTINEL_URL=http://localhost:9000

# External app integration (for "Go back to <app>" button)
# URL to redirect to when user clicks "Go back"
VITE_EXTERNAL_APP_URL=http://localhost:3000
# Name shown in the button: "Go back to <name>"
VITE_EXTERNAL_APP_NAME=Pitboss

# ── Dev Login ────────────────────────────────────────────────────────────────
# API token for external login exchange (pitboss-api).
VITE_TEST_API_TOKEN=sat_e91974dac53da17dd64b0d1bef35049542bd21ffbae0bc68cc7620ce32e5f936
# Email used by the "Join with Pitboss" quick-login button.
VITE_TEST_USER_EMAIL=anayamaster@gmail.com
# Optional: display name and avatar URL for the dev login.
VITE_TEST_DISPLAY_NAME=Eduardo Anaya
VITE_TEST_AVATAR_URL=https://example.com/avatar.png