[package]
name = "moshsplit-balance-engine"
version = "0.1.0"
edition = "2021"
description = "Pure Rust balance computation engine for MoshSplit"
authors = ["MoshSplit Team"]
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
uuid = { version = "1.0", features = ["v4", "serde"] }
